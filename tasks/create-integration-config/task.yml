platform: linux
inputs:
  - name: bbl-state
  - name: cf-deployment-concourse-tasks
  - name: metric-store-ci
outputs:
  - name: integration-config
params:
  BBL_STATE_DIR:
  SYSTEM_DOMAIN:
run:
  path: metric-store-ci/tasks/create-integration-config/task
