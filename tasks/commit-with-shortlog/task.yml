---
platform: linux

inputs:
- name: metric-store-ci # - This repo
- name: release-repo

outputs:
- name: committed-repo

run:
  path: metric-store-ci/tasks/commit-with-shortlog/task

params:
  # Required. The paths to commit.
  PATHS:
