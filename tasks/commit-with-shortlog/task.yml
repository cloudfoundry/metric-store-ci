---
platform: linux

inputs:
- name: log-cache-ci # - This repo
- name: release-repo

outputs:
- name: committed-repo

run:
  path: log-cache-ci/tasks/commit-with-shortlog/task

params:
  # Required. The paths to commit.
  PATHS:
